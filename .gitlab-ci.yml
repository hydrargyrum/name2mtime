stages:
    - lint
    - build

image: golang:1

govet:
    stage: lint
    script:
        - go vet *.go

build:
    stage: build
    script:
        - go build name2mtime.go
    artifacts:
        paths:
            - ./name2mtime
        expire_in: "1 month"
